# RAG Service Intro

This is a sample markdown document for ingestion v0.

It includes enough content to validate parsing, chunking, and storage.

The ingestion pipeline should be idempotent on re-runs.

This line was added to validate update behavior.
